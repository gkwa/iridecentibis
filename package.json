{
  "name": "iridecentibis",
  "version": "0.1.0",
  "description": "Obsidian plugin for grocery list management via Bases",
  "main": "main.js",
  "scripts": {
    "generate": "tsx scripts/generate-stores.ts",
    "build": "npm run generate && node esbuild.config.mjs production",
    "dev": "npm run generate && node esbuild.config.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "builtin-modules": "^4.0.0",
    "esbuild": "^0.24.0",
    "obsidian": "latest",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
